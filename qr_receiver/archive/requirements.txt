# QR Receiver System - Required Dependencies
# ==========================================
# Install with: pip install -r requirements.txt

# Core Dependencies
segno>=1.6.0                 # QR code generation and processing
pillow>=10.0.0               # Image processing

# Web Server and UI
aiohttp>=3.12.0              # Async web server framework
aiohttp-cors>=0.8.0          # CORS support for camera access

# Compression Support (matching qr_transfer)
brotli>=1.1.0                # Google's Brotli compression
zstandard>=0.24.0            # Facebook's Zstandard compression  
lz4>=4.4.0                   # LZ4 fast compression

# Security and Error Correction
cryptography>=41.0.0         # AES encryption
reedsolo>=1.7.0             # Reed-Solomon error correction
psutil>=5.9.0               # System monitoring for security

# Optional Development Dependencies
# pytest>=7.0.0             # Testing framework
# black>=23.0.0              # Code formatting
# mypy>=1.0.0                # Type checking

# Installation Notes:
# ===================
# This receiver system requires a web browser with camera access.
# Tested with Chrome, Firefox, Safari on desktop and mobile.
# 
# For air-gapped operation, install only core dependencies:
#   pip install segno pillow aiohttp aiohttp-cors
#
# For full functionality including compression and encryption:
#   pip install -r requirements.txt